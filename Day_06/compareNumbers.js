module.exports = function compareNumbers(num1, num2) {
    // TODO: 여기에 코드를 작성합니다.
    return num1 > num2 ? `${num1}은(는) ${num2}보다 큽니다` 
        : num1 < num2 ? `${num1}은(는) ${num2}보다 작습니다` 
        : `두 수는 같습니다`
}
